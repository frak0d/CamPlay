1. Create folder with same name as your Game's window title.
2. Train a "pose" model on https://teachablemachine.withgoogle.com/train/pose with apropriate class names.
3. Download it as tflite model and put inside the folder created in step 1 with exact name "pose_model".
4. Create keyboard actions corresponding to different pose classes in the text file "pose_action".